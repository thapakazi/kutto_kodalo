{
  "name": "kutto-kodalo-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Documentation site for kutto_kodalo. Content is generated from ../docs/.",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "dev": "npm run sync && astro dev",
    "start": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "0.41.7",
    "astro": "7.2.0"
  }
}
